---
pagination:
  data: mobileProfessions
  size: 1
  alias: mobileProfession
permalink: "mobile/{{ mobileProfession.slug }}/index.html"
eleventyComputed:
  title: "{{ mobileProfession.title | safe }}"
  description: "{{ mobileProfession.description | safe }}"
  keywords: "{{ mobileProfession.keywords | safe }}"
  canonical: "{{ mobileProfession.canonical | safe }}"
  pageName: "CueCard for {{ mobileProfession.name }}"
  ogTitle: "{{ mobileProfession.ogTitle | safe }}"
  ogDescription: "{{ mobileProfession.ogDescription | safe }}"
  ogUrl: "{{ mobileProfession.canonical | safe }}"
  ogLocale: "en_US"
layout: layouts/mobile-profession.njk
---
{% set roleFaq = faq.withGeneral(mobileProfession.faq) %}
{% block jsonld %}
<script type="application/ld+json">
{{ site.products.mobile | softwareSchema | jsonLd | safe }}
</script>
<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "@id": {{ ((mobileProfession.canonical) + "#faq") | jsonLd | safe }},
    "isPartOf": { "@id": {{ ((mobileProfession.canonical) + "#webpage") | jsonLd | safe }} },
    "inLanguage": "en",
    "mainEntity": [
        {% for item in roleFaq %}
        {
            "@type": "Question",
            "name": {{ item.question | jsonLd | safe }},
            "acceptedAnswer": { "@type": "Answer", "text": {{ item.answer | striptags | jsonLd | safe }} }
        }{% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}
</script>
{% endblock %}

{% block body %}
{% if mobileProfession.desktopCounterpart %}
{% set navCtaHref = "/" + mobileProfession.desktopCounterpart + "/" %}
{% else %}
{% set navCtaHref = "/desktop/" %}
{% endif %}
{% set navCtaText = "Presenting from a Mac or PC instead? CueCard keeps your speaker notes out of your screen share" %}
{% set navCtaTextMobile = "Also for Mac and Windows" %}
{% include "partials/nav.njk" %}

<main>
    {% set heroClass = "hero-app" %}
    {% set heroTitle = mobileProfession.heroSubtitle %}
    {% set heroLong = true %}
    {% set heroBody = "A floating teleprompter built for " + mobileProfession.targetAudience + ". Your script sits on top of whatever app you are recording in and scrolls while you talk, so you can keep your eyes up and get the take in one go." %}
    {% set heroButtons = [
        {
            text: "Get it free on iPhone & iPad",
            href: site.ios,
            icon: "apple",
            style: "primary",
            external: true
        },
        { text: "Watch the demo", href: "#demo", icon: "play", style: "ghost" }
    ] %}
    {% set heroNote = 'Free · ' + site.requiresMobileShort + ' · <a href="/mobile/android/">Android coming soon</a>' %}
    {% set heroTagline = "No more forgetting your lines or switching apps mid-take." %}
    {% set heroStrip = "social" %}
    {% set stripMore = "and every other app you record in" %}
    {% set faqLink = "#faq" %}
    {% set heroDeck = true %}
    {% set deckScript = deck.forMobileRole(mobileProfession.name, mobileProfession.targetAudience, mobileProfession.useCases[0]) %}
    {% include "partials/hero.njk" %}

    {% set featureRows = features.forRole(mobileProfession.name, mobileProfession.targetAudience, "mobile") %}
    {% set rowsMicro = "What you get" %}
    {% set rowsHeading = "Everything you need to stay on script" %}
    {% set rowsId = "features" %}
    {% include "partials/rows.njk" %}

    {% set shotsHeading = "This is the whole app." %}
    {% set shotsNote = "Read your script from the full screen, or shrink it into a small window that stays on top of whatever else you open." %}
    {% set shotsFoot = 'Looking for the computer app instead? <a href="/desktop/">CueCard for Mac and Windows</a> keeps your speaker notes on your screen and out of your screen share.' %}
    {% include "partials/shots.njk" %}

    {% set watchHeading = "A minute of it, in use" %}
    {% set watchNote = "Short films of the app doing the one thing it is for. Each opens on YouTube." %}
    {% include "partials/watch.njk" %}

    {% set usesHeading = "What " + mobileProfession.name | lower + " use it for" %}
    {% set useItems = mobileProfession.useCases %}
    {% if mobileProfession.desktopCounterpart %}
    {% set usesFoot = 'Presenting rather than filming? There are <a href="/' + mobileProfession.desktopCounterpart + '/">invisible speaker notes for ' + mobileProfession.name | lower + ' on a Mac or PC</a> as well.' %}
    {% else %}
    {% set usesFoot = false %}
    {% endif %}
    {% include "partials/uses.njk" %}

    {% set bigscreenRole = mobileProfession.name %}
    {% include "partials/bigscreen.njk" %}

    {% set gridRoles = site.mobileRoles %}
    {% set gridBase = "/mobile/" %}
    {% set gridId = "roles" %}
    {% set gridMicro = "Made for" %}
    {% set gridHeading = "Whoever else has to say it on camera" %}
    {% set gridSuffix = "teleprompter app" %}
    {% include "partials/rolegrid.njk" %}

    {% set crossRole = mobileProfession.name %}
    {% include "partials/crosspanel.njk" %}

    {% set getHeading = "On your phone in under a minute" %}
    {% set getBody = "Free, with no subscription and nothing to cancel — and free on your computer too." %}
    {% include "partials/getcuecard.njk" %}

    {% set guide = mobileGuides.forRole(mobileProfession) %}
    {% include "partials/setup-guide.njk" %}

    {% set faqItems = roleFaq %}
    {% set faqHeading = "Questions from " + mobileProfession.name | lower %}
    {% include "partials/faq.njk" %}
</main>

{% include "partials/footer.njk" %}
{% endblock %}
