---
pagination:
  data: platforms
  size: 1
  alias: platform
permalink: "{{ platform.slug }}/index.html"
eleventyComputed:
  title: "{{ platform.title | safe }}"
  description: "{{ platform.description | safe }}"
  keywords: "{{ platform.keywords | safe }}"
  canonical: "{{ platform.canonical | safe }}"
  pageName: "{{ platform.jsonLdName | safe }}"
  ogTitle: "{{ platform.ogTitle | safe }}"
  ogDescription: "{{ platform.ogDescription | safe }}"
  ogUrl: "{{ platform.canonical | safe }}"
  ogLocale: "en_US"
layout: layouts/platform.njk
---
