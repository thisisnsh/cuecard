# CueCard Teleprompter — a free teleprompter for iPhone and iPad that floats
# over every other app, and speaker notes that stay invisible while you share
# your screen on a Mac or PC.
# https://cuecard.dev

User-agent: *
Allow: /

# /ios/ and /android/ are meta-refresh stubs pointing at the app stores, and
# they carry noindex. They are deliberately NOT disallowed here: a crawler that
# is blocked from fetching them never reads the noindex, and can still list the
# bare URL. Letting it fetch the stub is what gets them dropped.

Sitemap: https://cuecard.dev/sitemap.xml
